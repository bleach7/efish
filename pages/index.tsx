import { HomeContent } from "@/components";
import { LandingLayout } from "@/layouts";

const Home = () => {
  return (
    <LandingLayout>
      <HomeContent />
    </LandingLayout>
  );
};

export default Home;
