import { LoginContent } from "@/components";
import { LoginLayout } from "@/layouts";

const Login = () => {
  return (
    <LoginLayout>
      <LoginContent />
    </LoginLayout>
  );
};

export default Login;
